# local-llm-hub — local LLM infra: Ollama + LiteLLM + Qdrant (RAG) + Phoenix (observability).
# Bring up:  docker compose up -d
services:
  ollama:
    image: ollama/ollama:0.32.0@sha256:57f573b47f1f71ebb445789f279fe3e596a8beab182f7cf486db9205bad87c5a            # pinned version + digest, never "latest"
    container_name: ollama
    restart: unless-stopped
    ports:
      - "127.0.0.1:11434:11434"            # Ollama's own API, loopback only (debug/pull)
    volumes:
      - ollama-data:/root/.ollama          # named volume: downloaded models survive restarts
    # GPU is opt-in: apply docker-compose.gpu.yml to pass /dev/dri + /dev/kfd through.
    healthcheck:
      test: ["CMD-SHELL", "ollama list >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 15s

  litellm:
    image: ghcr.io/berriai/litellm:v1.92.0@sha256:9ef6f45bc0104940571765e610c52a1d761b5ec85efcd193795281086ee61277
    container_name: litellm
    restart: unless-stopped
    command: ["--config", "/app/config.yaml", "--port", "4000", "--num_workers", "1"]
    ports:
      - "127.0.0.1:4000:4000"              # THE single public entry point
    volumes:
      - ./litellm_config.yaml:/app/config.yaml:ro   # bind-mount our config, read-only
    env_file: [.env]                       # master key injected from here (gitignored)
    environment:                           # send every request's trace to Phoenix via OTel/OTLP
      OTEL_EXPORTER_OTLP_ENDPOINT: http://phoenix:6006
      OTEL_EXPORTER_OTLP_PROTOCOL: http/protobuf
      OTEL_SERVICE_NAME: litellm
    depends_on:
      ollama:
        condition: service_healthy         # don't start LiteLLM until Ollama is ready
      phoenix:
        condition: service_started
    healthcheck:
      # python ships in the litellm image; curl is not
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:4000/health/liveliness', timeout=3)\" || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 6
      start_period: 25s

  qdrant:
    image: qdrant/qdrant:v1.18.2@sha256:75eab8c4ba42096724fdcfde8b4de0b5713d529dde32f285a1f86fdcb2c9e50c          # pinned latest stable (2026-06) + digest
    container_name: qdrant
    restart: unless-stopped
    ports:
      - "127.0.0.1:6333:6333"             # REST + web dashboard
      - "127.0.0.1:6334:6334"             # gRPC (faster for bulk upserts)
    volumes:
      - qdrant-data:/qdrant/storage       # collections + vectors persist here
    healthcheck:                          # Qdrant image ships bash; if this errors, delete the block
      test: ["CMD-SHELL", "bash -c '</dev/tcp/localhost/6333'"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s

  open-webui:                               # ChatGPT-style web UI; browser → here → LiteLLM
    image: ghcr.io/open-webui/open-webui:main@sha256:a26effeb220e132482bf7e0560b3404843e7bc40d23051144e062960df8df6b0
    container_name: open-webui
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"                # Web UI (browser); loopback only like the rest
    volumes:
      - open-webui-data:/app/backend/data    # users, chats, settings + WEBUI_SECRET_KEY persist here
    environment:
      # Route through LiteLLM (not Ollama directly) so every chat hits the
      # master-key gate + Phoenix tracing. DISABLE_OLLAMA_API prevents the
      # direct-Ollama shortcut that would bypass both.
      OPENAI_API_BASE_URL: http://litellm:4000/v1
      OPENAI_API_KEY: ${LITELLM_MASTER_KEY}  # compose-substituted from .env at parse time
      ENABLE_OLLAMA_API: "false"
      # Disable builtin tools (tasks, timestamps, KB search...) by default.
      # Open WebUI injects them into every chat via the function-calling prompt;
      # small models like llama3.2 (3B) hallucinate tool-call JSON instead of
      # chatting. Re-enable per-model in the admin UI for models that can handle it.
      DEFAULT_MODEL_METADATA: '{"capabilities":{"builtin_tools":false}}'
      # Auth: default ENABLE_SIGNUP=true. First browser signup becomes admin;
      # disable signup afterward in Admin Panel > Settings to lock to one account.
    depends_on:
      litellm:
        condition: service_healthy           # model list is fetched from LiteLLM on startup
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:8080/health', timeout=3)\" || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 6
      start_period: 30s

  phoenix:
    image: arizephoenix/phoenix:18.1.0@sha256:3703cf74388218163d4b599892c4c47560a6003b117d68896963a88898fe7815      # pinned (arize-phoenix v18.1.0) + digest
    container_name: phoenix
    restart: unless-stopped
    environment:
      PHOENIX_ENABLE_AUTH: "false"          # local single-user; UI + OTLP open on loopback only
    ports:
      - "127.0.0.1:6006:6006"               # Phoenix UI (browser); OTLP 4317/4318 are internal-only
    volumes:
      - phoenix-data:/mnt/data              # embedded SQLite store persists here

volumes:
  ollama-data:
  qdrant-data:
  phoenix-data:
  open-webui-data:

# Stack: ollama + litellm + qdrant + phoenix + open-webui — all bound to 127.0.0.1.
