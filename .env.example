# Copy to .env and fill real values. .env is gitignored — never commit.
# Generate each key:  openssl rand -hex 32
LITELLM_MASTER_KEY=sk-REPLACE_WITH_64_HEX_CHARS   # clients send as the Bearer token
LITELLM_SALT_KEY=sk-REPLACE_WITH_DIFFERENT_64_HEX_CHARS  # salts/hashes future virtual keys
